#include "main.h"

/**
 * _isdigit - checks for digits
 * @c: the variable to be checked
 * Return: i if its a digit and 0 otherwise
 */

int _isdigit(int c)
{
	if (x >= 48 && c <= 57)
	{
		return (1);
	}
	return (0);
}
